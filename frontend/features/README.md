Feature folders hold feature-specific services, types, and helper modules.
Structure suggestion:

- features/<feature>/services
- features/<feature>/components
- features/<feature>/types
  Note: Routing pages remain under `app/` to preserve Next.js routing. Services and repos are moved into `services/` and `repositories/` respectively.
